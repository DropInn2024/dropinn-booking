-- ============================================================
-- 代收行程費「已收勾記」：addonCollected
-- 用來追蹤「跟客人收到代訂行程費了沒」（與付給旅行社的月結成本無關）。
-- 0 = 還沒跟客人收（進首頁未收提醒）；1 = 已收。
-- 純內部欄位，不外洩、不進公開 API。
-- 只加欄位 → 可進 repo。
-- ============================================================
ALTER TABLE orders ADD COLUMN addonCollected INTEGER DEFAULT 0;
