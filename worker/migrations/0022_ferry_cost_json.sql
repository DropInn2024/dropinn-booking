-- ============================================================
-- 船票（複雜計價）用：cost_json 存多維成本（平假連 × 來回單程 × 全半嬰 + 接駁）
-- ------------------------------------------------------------
-- 客報價放 meta（公開 API 會回）；成本放 cost_json（公開 API 絕不 SELECT）。
-- 只加欄位，無成本數字 → 可進 repo。
-- ============================================================
ALTER TABLE tour_products ADD COLUMN cost_json TEXT DEFAULT '';
